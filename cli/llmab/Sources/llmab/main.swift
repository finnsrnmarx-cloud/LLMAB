import Foundation
import LLMCore

// Chunk 1: placeholder entry point.
// swift-argument-parser + real subcommands (`chat`, `models`, `pull`) land in chunk 6.
print("ω llmab \(LLMCore.version) — scaffold. Subcommands arrive in chunk 6.")
